Verify the implementation by running the appropriate test or lint command for this project.

If no real automated check exists yet, explain that clearly and emit `VERDICT: FAIL: <reason>` so the workflow can bounce until a concrete check is in place.

If the relevant command succeeds and the implementation looks correct for this phase, emit `VERDICT: PASS`.
